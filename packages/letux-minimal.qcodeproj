#!/bin/bash
# autogenerated by QuantumCode
[ "$QuantumSTEP" ] || export QuantumSTEP="/usr/local/QuantumSTEP"
export DEBIAN_ARCHITECTURES="all"
export DEBIAN_DEPENDS="etch:sudo"
export DEBIAN_DEPENDS+=", squeeze:sudo"
export DEBIAN_DEPENDS+=", lenny:sudo"
export DEBIAN_DEPENDS+=", wheezy:sudo"
export DEBIAN_DEPENDS+=", jessie:sudo"
export DEBIAN_DEPENDS+=", stretch:sudo"
export DEBIAN_DEPENDS+=", bullseye:sudo"
export DEBIAN_DEPENDS+=", bookworm:sudo"
export DEBIAN_DEPENDS+=", trixie:sudo"
export DEBIAN_DEPENDS+=", forky:sudo"
export DEBIAN_DEPENDS+=", jessie:letux-system-d"
export DEBIAN_DEPENDS+=", stretch:net-tools"
export DEBIAN_DEPENDS+=", buster:net-tools"
export DEBIAN_DEPENDS+=", bullseye:net-tools"
export DEBIAN_DEPENDS+=", bookworm:net-tools"
export DEBIAN_DEPENDS+=", trixie:net-tools"
export DEBIAN_DEPENDS+=", forky:net-tools"
export DEBIAN_DEPENDS+=", letux-sources-list"
export DEBIAN_DEPENDS+=", letux-archive-keyring"
export DEBIAN_DEPENDS+=", ntp"
export DEBIAN_DEPENDS+=", udev"
export DEBIAN_DEPENDS+=", dnsutils"
export DEBIAN_DEPENDS+=", openssh-server"
export DEBIAN_DEPENDS+=", openssh-client"
export DEBIAN_DEPENDS+=", ca-certificates"
export DEBIAN_DEPENDS+=", squeeze:apt-transport-https"
export DEBIAN_DEPENDS+=", lenny:apt-transport-https"
export DEBIAN_DEPENDS+=", wheezy:apt-transport-https"
export DEBIAN_DEPENDS+=", jessie:apt-transport-https"
export DEBIAN_DEPENDS+=", stretch:apt-transport-https"
export DEBIAN_DEPENDS+=", bullseye:apt-transport-https"
export DEBIAN_DEPENDS+=", bookworm:apt-transport-https"
export DEBIAN_DEPENDS+=", trixie:apt-transport-https"
export DEBIAN_DEPENDS+=", forky:apt-transport-https"
export DEBIAN_DEPENDS+=", debian-keyring"
export DEBIAN_DEPENDS+=", locales"
export DEBIAN_DEPENDS+=", time"
export DEBIAN_DEPENDS+=", rsync"
export DEBIAN_DEPENDS+=", daemon"
export DEBIAN_DEPENDS+=", alsa-utils"
export DEBIAN_DEPENDS+=", bzip2"
export DEBIAN_DEPENDS+=", parted"
export DEBIAN_DESCRIPTION="Config for Letux minimal"
export DEBIAN_PACKAGE_NAME="letux-minimal"
export DEBIAN_SECTION="misc"
export INSTALL_PATH="/"
export PRODUCT_NAME="letux-minimal"

cd "$(dirname "${BASH_SOURCE[0]}")" && "$QuantumSTEP/System/Sources/Frameworks/mySTEP.make" $ACTION
